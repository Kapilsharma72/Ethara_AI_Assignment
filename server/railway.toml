[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build --workspace=server"

[deploy]
startCommand = "node server/dist/db/migrate.js && node server/dist/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
